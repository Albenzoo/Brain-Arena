meta {
  name: random
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/questions/random?lang=it
  body: none
  auth: inherit
}

params:query {
  lang: it
}

settings {
  encodeUrl: true
  timeout: 0
}
