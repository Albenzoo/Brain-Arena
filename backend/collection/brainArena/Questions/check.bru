meta {
  name: check
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/questions/check?lang=en
  body: json
  auth: inherit
}

params:query {
  lang: en
}

body:json {
  {
    "questionId": 1,
    "selectedAnswer": "Mercury"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
